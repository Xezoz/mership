-- Temporarily disable RLS to allow direct editing
ALTER TABLE profiles DISABLE ROW LEVEL SECURITY;

-- Now you can edit the balance directly in the table editor
-- After you're done testing, re-enable RLS with:
-- ALTER TABLE profiles ENABLE ROW LEVEL SECURITY;
